info:
  name: verify soft-deleted entities via pull
  type: http
  seq: 6

http:
  method: post
  url: https://script.google.com/macros/s/{{deploymentId}}/exec#pull
  headers:
    - name: accept
      value: application/json
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "action": "pull",
        "versions": {
          "tasks": 0,
          "goals": 0,
          "contexts": 0,
          "categories": 0,
          "checklist_items": 0
        }
      }
  auth: inherit

runtime:
  scripts:
    - type: tests
      code: |-
        const data = res.getBody().data;

        const isPresentAndDeleted = (list, id) => {
          const item = list?.find(i => i.id === id);
          return !!(item && item.is_deleted);
        };

        test("task should be present and deleted", function() {
          expect(isPresentAndDeleted(data.tasks, bru.getEnvVar("taskId"))).to.be.true;
        });

        test("goal should be present and deleted", function() {
          expect(isPresentAndDeleted(data.goals, bru.getEnvVar("goalId"))).to.be.true;
        });

        test("context should be present and deleted", function() {
          expect(isPresentAndDeleted(data.contexts, bru.getEnvVar("contextId"))).to.be.true;
        });

        test("category should be present and deleted", function() {
          expect(isPresentAndDeleted(data.categories, bru.getEnvVar("categoryId"))).to.be.true;
        });

        test("checklist item should be present and deleted", function() {
          expect(isPresentAndDeleted(data.checklist_items, bru.getEnvVar("checklistItemsId"))).to.be.true;
        });
  assertions:
    - expression: res.status
      operator: eq
      value: "200"
    - expression: res.body.ok
      operator: isTruthy

settings:
  encodeUrl: false
  timeout: 0
  followRedirects: true
  maxRedirects: 5
